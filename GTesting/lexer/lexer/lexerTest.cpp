#include "stdafx.h"
#include <iostream>
#include "lexer.h"
#include "gtest/gtest.h"
#include <string.h>

/*
TEST(FileLexer, FileLexTest)
{
	ASSERT_NO_THROW(lexFile("TestFile.txt"));
}

TEST(FileLexerLine, FileLexLineTest)
{
	ASSERT_NO_THROW(lexInputLine("CREATE TABLE animals (name VARCHAR(20), kind VARCHAR(8), years INTEGER) PRIMARY KEY (name, kind);"));
}


int main(int argc, char** argv)
{
	testing::InitGoogleTest(&argc, argv);
	RUN_ALL_TESTS();
	std::getchar(); // keep console window open until Return keystroke
}

*/